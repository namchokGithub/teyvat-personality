# Missing Data Report

Generated by the character data importer. Lists fields left `null` because neither `genshin-db/` nor `paimon-moe/` had a reliable value (never invented).

character files have at least one missing field.

| id                 | missing fields                          |
| ------------------ | --------------------------------------- |
| `aloy`             | region                                  |
| `nicole`           | region                                  |
| `skirk`            | region                                  |
| `traveler_anemo`   | title, titleTh, descriptionTh, birthday |
| `traveler_dendro`  | title, titleTh, descriptionTh, birthday |
| `traveler_electro` | title, titleTh, descriptionTh, birthday |
| `traveler_geo`     | title, titleTh, descriptionTh, birthday |
| `traveler_hydro`   | title, titleTh, descriptionTh, birthday |
| `traveler_pyro`    | title, titleTh, descriptionTh, birthday |
| `traveler_cryo`    | title, titleTh, descriptionTh, birthday |

Notes:

- `traveler_*` — `gender` is intentionally `null` (Traveler is a twin: Aether = Male, Lumine = Female) and is not listed as a gap here.
- `traveler_*` — `titleTh`/`descriptionTh` have no source: `genshin-db` has no per-element Traveler file in Thai either (only `aether.json`/`lumine.json`, both blank), and `paimon-moe` has no Thai text.
- `aloy`, `nicole`, `skirk`, `zibai` — `region` is blank in `genshin-db` itself, in both English and Thai (these characters are not affiliated with one of the seven nations in-game); left `null` rather than guessed.
