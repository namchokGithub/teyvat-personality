# Missing Data Report

Generated by the character data importer. Lists fields left `null` because neither `genshin-db/` nor `paimon-moe/` had a reliable value (never invented).

character files have at least one missing field.

| id         | missing fields |
| ---------- | -------------- |
| `aloy`     | region         |
| `nicole`   | region         |
| `skirk`    | region         |

Notes:

- `traveler_*` — shared fields are `title: "Descender"`, `titleTh: "ผู้มาเยือน"`, and the official English/Thai profile descriptions. `birthday` remains `null` because it is the player's choice; `gender` remains `null` because it depends on the selected twin (Aether/Lumine). These are intentional dynamic values, not gaps.
- `aloy`, `nicole`, `skirk` — no reliable affiliation with one of the seven nations is available; `region` remains `null` rather than guessed.
